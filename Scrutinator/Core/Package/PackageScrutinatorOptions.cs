namespace Scrutinator.Core.Package;

public class PackageScrutinatorOptions
{
    /// <summary>
    /// If true, opens the default browser to the dashboard on app start (Dev only).
    /// </summary>
    public bool OpenDashboardAutomatically { get; set; } = true;
}